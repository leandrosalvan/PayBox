services:
  - type: web
    name: paybox
    env: node
    plan: free
    buildCommand: bash -c "npm install && set -x; echo 'DATABASE_URL is set:' \${DATABASE_URL:+yes}; sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/g' prisma/schema.prisma && npx prisma@5.22.0 generate && npx prisma@5.22.0 db push --accept-data-loss && npm run build"
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NEXTAUTH_URL
        value: https://paybox-8j56.onrender.com
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: BREVO_SMTP_HOST
        value: smtp-relay.brevo.com
      - key: BREVO_SMTP_PORT
        value: "587"
      - key: BREVO_SMTP_USER
        sync: false
      - key: BREVO_SMTP_PASS
        sync: false
      - key: FROM_EMAIL
        sync: false
      - key: PAYBOX_MCP_TOKEN
        sync: false
      - key: PAYBOX_MCP_USER_EMAIL
        sync: false
      - key: PAYBOX_MCP_SCOPES
        value: read
      - key: PAYBOX_MCP_WRITE_ENABLED
        value: "false"
